module.exports = {
    name: 'rule',
    aliases: ['rules'],
    category: 'general',
    exp: 5,
    cool: 30,
    react: "✅",
    description: 'Bot rules',
    async execute(client, arg, M) {

        let rules = `No Rules you fool`
        M.reply(rules)

        
    }
}
